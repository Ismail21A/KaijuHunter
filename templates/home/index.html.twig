{% extends 'base.html.twig' %}

{% block title %}KaijuHunter — Accueil{% endblock %}

{% block body %}
<div class="container py-5">

    {# Hero principal #}
    <div class="row align-items-center mb-5">
        <div class="col-md-7">
            <h1 class="display-5 fw-bold mb-3">KaijuHunter</h1>
            <p class="lead text-muted mb-4">
                Gérez vos <strong>vitrines</strong>, vos <strong>figures</strong> et vos
                <strong>arenas</strong> dans un univers inspiré de <em>Kaiju No. 8</em> et
                <em>Solo Leveling</em>.
            </p>

            <div class="d-flex flex-wrap gap-2">
                <a href="{{ path('app_arena_index') }}" class="btn btn-primary btn-lg">
                    Explorer les arenas publiques
                </a>

                {% if app.user %}
                    <a href="{{ path('app_member_show', { id: app.user.id }) }}"
                       class="btn btn-outline-secondary btn-lg">
                        Aller dans mon espace
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-secondary btn-lg">
                        Me connecter
                    </a>
                {% endif %}
            </div>
        </div>

        <div class="col-md-5 mt-4 mt-md-0">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h2 class="h5 mb-3">Comptes de démonstration</h2>
                    <p class="small text-muted mb-2">
                        Utilisez ces identifiants pour tester KaijuHunter :
                    </p>
                    <ul class="list-unstyled small mb-0">
                        <li>
                            <strong>Admin</strong> :
                            <code>admin@localhost</code> / <code>admin123</code>
                        </li>
                        <li>
                            <strong>Membre</strong> :
                            <code>olivier@localhost</code> / <code>123456</code>
                        </li>
                        <li>
                            <strong>Membre</strong> :
                            <code>slash@localhost</code> / <code>123456</code>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    {# Arenas publiques en avant #}
    {% if arenas is defined and arenas %}
        <h2 class="h4 mb-3">Arenas publiques en avant</h2>

        <div class="row g-4">
            {% for arena in arenas %}
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex flex-column">
                            <h3 class="h5 mb-2">
                                {{ arena.description }}
                            </h3>

                            <p class="small text-muted mb-2">
                                {% if arena.owner %}
                                    Par {{ arena.owner.email }}
                                {% else %}
                                    Propriétaire inconnu
                                {% endif %}
                            </p>

                            <span class="badge {{ arena.publie ? 'bg-success' : 'bg-secondary' }} mb-3">
                                {{ arena.publie ? 'Publique' : 'Privée' }}
                            </span>

                            <a href="{{ path('app_arena_show', { id: arena.id }) }}"
                               class="btn btn-sm btn-outline-primary mt-auto">
                                Voir l'arena
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-light border d-flex align-items-center">
            <i class="bi bi-info-circle me-2"></i>
            <div class="small text-muted">
                Aucune arena publique en avant pour le moment.
                Connectez-vous avec <strong>olivier@localhost</strong> ou
                <strong>slash@localhost</strong> pour en créer.
            </div>
        </div>
    {% endif %}

</div>
{% endblock %}
